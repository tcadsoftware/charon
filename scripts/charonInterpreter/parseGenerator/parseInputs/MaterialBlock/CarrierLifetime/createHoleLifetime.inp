

interpreter name HoleLifetime

interpreter inputLine (hole lifetime is constant) = {lifetime} 

interpreter shortHelp {Specify an explicit value for hole lifetime.}

interpreter longHelp {Specify an explicit value for hole lifetime. <> {lifetime} is the lifetime in seconds.}

xmlRequired Charon->Closure Models->{MaterialBlockName}->Hole Lifetime,Value,double,{lifetime}

