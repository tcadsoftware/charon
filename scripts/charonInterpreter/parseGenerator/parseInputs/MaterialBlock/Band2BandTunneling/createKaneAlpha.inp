interpreter name KaneAlpha  

interpreter inputLine (Kane alpha) = {value} 

interpreter shortHelp {Set the Kane alpha parameter for the Band2Band tunneling local model}

interpreter longHelp {Set the Kane alpha parameter for the Band2Band tunneling local model}

xmlRequired Charon->Closure Models->{MaterialBlockName}->Band2Band Tunneling,Kane alpha,double,{value}
