interpreter name HurkxAlpha  

interpreter inputLine (Hurkx alpha) = {value} 

interpreter shortHelp {Set the Hurkx alpha parameter for the Band2Band tunneling local model}

interpreter longHelp {Set the Hurkx alpha parameter for the Band2Band tunneling local model}

xmlRequired Charon->Closure Models->{MaterialBlockName}->Band2Band Tunneling,Hurkx alpha,double,{value}
