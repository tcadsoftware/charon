
interpreter name CaptureCrossSection

interpreter inputLine (capture cross section) = {sigma}

interpreter shortHelp {Sets the volume trap capture cross section for the TID model}

interpreter longHelp {Sets the volume trap capture cross section for the TID model}

xmlRequired Charon->Closure Models->{MaterialBlockName}->TID->Kimpton Model->Volume Traps,Capture Cross Section,double,{sigma}